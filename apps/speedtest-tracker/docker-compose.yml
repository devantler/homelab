networks:
  traefik:
    external: true
  internal:
    external: true
services:
  speedtest-tracker:
    environment:
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: speedtest_tracker
      DB_USERNAME: ${DB_USERNAME?}
      DB_PASSWORD: ${DB_PASSWORD?}
    image: ghcr.io/alexjustesen/speedtest-tracker:v0.11.1
    labels:
      traefik.enable: 'true'
      traefik.http.routers.speedtest.entrypoints: ${ENTRYPOINT?}
      traefik.http.routers.speedtest.rule: Host(`speedtest.${DOMAIN?}`)
    networks:
      - traefik
      - internal
    restart: unless-stopped
    volumes:
      - speedtest-tracker-config:/config
volumes:
  speedtest-tracker-config: null
