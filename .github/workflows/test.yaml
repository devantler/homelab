name: Test

on:
  pull_request:
    branches: [main]
    # Added 'edited' temporarily, as there currently is no trigger for when the base branch is updated.
    # https://github.com/orgs/community/discussions/64119
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
      - closed
      - edited
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: homelab
    if: github.event.pull_request.draft == false
    steps:
      - name: 📑 Checkout
        uses: actions/checkout@v4
      - name: ⚙️ Install wget
        run: sudo apt-get install -y wget
      - name: ⚙️ Install KSail
        run: |
          ls -l $HOME/
          ls -l $HOME/.local/bin
          wget -q "https://getbin.io/devantler/ksail" -o $HOME/.local/bin/ksail
          chmod +x $HOME/.local/bin/ksail
      - name: ⛴️ Provision cluster
        run: |
          ksail sops ${{ vars.KSAIL_CLUSTER_NAME }} --import "${{ secrets.KSAIL_SOPS_KEY }}"
          ksail up ${{ vars.KSAIL_CLUSTER_NAME }}
      - name: 🔥 Teardown cluster
        if: always()
        run: ksail down ${{ vars.KSAIL_CLUSTER_NAME }}
