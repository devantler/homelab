name: Lint Check With kube-score
on: push
jobs:
  lint-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: kubectl apply -k ./cluster/ --recursive --dry-run=client
