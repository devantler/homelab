name: Datree Linter
on: push
env:
  DATREE_TOKEN: ${{ secrets.DATREE_TOKEN }} 
jobs:
  datree-linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: chmod +x ci/datree-linter-multiple-files.sh
      - run: ci/datree-linter-multiple-files.sh
