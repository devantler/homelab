apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: cluster-ci
volumes:
  - volume: cluster-ci:/tmp/k3dvol
ports:
  - port: 8443:443 # HTTPS
options:
  k3s:
    extraArgs:
      - arg: "--disable=traefik"
        nodeFilters:
          - server:*
