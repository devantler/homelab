apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: cluster-production
volumes:
  - volume: cluster-production:/tmp/k3dvol
ports:
  - port: 80:80
  - port: 443:443
options:
  k3s:
    extraArgs:
      - arg: "--disable=traefik"
        nodeFilters:
          - server:*
  