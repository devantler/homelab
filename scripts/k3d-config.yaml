apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: cluster-local
volumes:
  - volume: cluster-local:/tmp/k3dvol
ports:
  - port: 8443:443
options:
  k3s:
    extraArgs:
      - arg: "--disable=traefik"
        nodeFilters:
          - server:*