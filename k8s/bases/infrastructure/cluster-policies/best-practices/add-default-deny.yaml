# Generates default-deny network policies + a DNS-allow policy in every
# namespace. This ensures zero-trust networking by default — workloads must
# explicitly allow the traffic they need.
#
# BOTH a CiliumNetworkPolicy and a standard Kubernetes NetworkPolicy default-deny
# are generated. Cilium enforces both policy types and combines them ADDITIVELY:
# the effective allow-set for an endpoint is the UNION across every policy source
# (standard NetworkPolicy + CiliumNetworkPolicy + CiliumClusterwideNetworkPolicy).
# A pure standard default-deny (podSelector {}, no allow rules) therefore does NOT
# block traffic that a CiliumNetworkPolicy explicitly allows — it only contributes
# "isolate these pods", which the Cilium default-deny below already does.
#
# Verified in production: the wedding-app namespace runs exactly this pure standard
# default-deny alongside its CiliumNetworkPolicy allows and serves traffic normally.
# The standard NetworkPolicy is required because scanners and the CIS benchmark
# (Kubescape C-0030/C-0260/C-0206) do not recognise CiliumNetworkPolicy as a
# NetworkPolicy, so without it the cluster's real segmentation is invisible to them.
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: add-default-deny
  annotations:
    policies.kyverno.io/title: Default Deny Network Policy
    policies.kyverno.io/category: Networking, Best Practices
    policies.kyverno.io/subject: NetworkPolicy, CiliumNetworkPolicy
    policies.kyverno.io/minversion: 1.6.0
    policies.kyverno.io/description: >-
      Generates a CiliumNetworkPolicy that activates Cilium's whitelist
      mode for all endpoints in a namespace (effectively deny-all), a
      companion CiliumNetworkPolicy that allows DNS egress to kube-dns so
      pods can still resolve names, and a standard Kubernetes NetworkPolicy
      default-deny so the segmentation is visible to NetworkPolicy-aware
      scanners and the CIS benchmark.
spec:
  rules:
    - name: generate-default-deny
      match:
        any:
          - resources:
              kinds:
                - Namespace
      exclude:
        any:
          - resources:
              names:
                - kube-system
                - kube-public
                - kube-node-lease
      generate:
        generateExisting: true
        apiVersion: cilium.io/v2
        kind: CiliumNetworkPolicy
        name: default-deny
        synchronize: true
        namespace: "{{request.object.metadata.name}}"
        data:
          spec:
            endpointSelector: {}
            # Selecting every endpoint activates whitelist mode. Keep this as
            # default-deny-by-selection: an explicit ingressDeny/egressDeny
            # rule would take precedence over the namespace's allow policies.
            # Cilium's CRD also requires at least one policy direction to be
            # present, so retain empty allow lists for both directions.
            ingress: []
            egress: []
            enableDefaultDeny:
              ingress: true
              egress: true
    - name: generate-allow-dns
      match:
        any:
          - resources:
              kinds:
                - Namespace
      exclude:
        any:
          - resources:
              names:
                - kube-system
                - kube-public
                - kube-node-lease
      generate:
        generateExisting: true
        apiVersion: cilium.io/v2
        kind: CiliumNetworkPolicy
        name: allow-dns
        synchronize: true
        namespace: "{{request.object.metadata.name}}"
        data:
          spec:
            endpointSelector: {}
            egress:
              - toEndpoints:
                  - matchLabels:
                      k8s:io.kubernetes.pod.namespace: kube-system
                      k8s-app: kube-dns
                toPorts:
                  - ports:
                      - port: "53"
                        protocol: UDP
                      - port: "53"
                        protocol: TCP
    # The CloudNativePG operator runs in cnpg-system and connects directly to each
    # instance pod — 8000 for the instance manager's status endpoint, 5432 for
    # PostgreSQL. That path crosses a namespace boundary, and
    # restrict-tenant-network-policies refuses a tenant selector that names another
    # namespace, so a tenant CANNOT declare it: a selector carrying no namespace key
    # is scoped to the policy's own namespace and can never select the operator's
    # pods. The platform therefore generates it, for the same reason it generates
    # allow-dns — a platform-required cross-namespace path the tenant may not name.
    # The destination is pinned to CNPG instance pods (every one carries
    # `cnpg.io/cluster`) rather than to the whole namespace, so a namespace running
    # no database gains no reachable path.
    - name: generate-allow-cnpg-operator
      match:
        any:
          - resources:
              kinds:
                - Namespace
      exclude:
        any:
          - resources:
              names:
                - kube-system
                - kube-public
                - kube-node-lease
      generate:
        generateExisting: true
        apiVersion: cilium.io/v2
        kind: CiliumNetworkPolicy
        name: allow-cnpg-operator
        synchronize: true
        namespace: "{{request.object.metadata.name}}"
        data:
          spec:
            endpointSelector:
              matchExpressions:
                - key: k8s:cnpg.io/cluster
                  operator: Exists
            ingress:
              - fromEndpoints:
                  - matchLabels:
                      k8s:io.kubernetes.pod.namespace: cnpg-system
                toPorts:
                  - ports:
                      - port: "8000"
                        protocol: TCP
                      - port: "5432"
                        protocol: TCP
    # Standard Kubernetes NetworkPolicy mirror of the Cilium default-deny above.
    # Pure deny (no allow rules); Cilium unions allows across both policy types,
    # so real traffic keeps flowing via the CiliumNetworkPolicies. This exists so
    # NetworkPolicy-aware scanners (Kubescape C-0030/C-0260/C-0206) can see that
    # every namespace is segmented.
    - name: generate-default-deny-networkpolicy
      match:
        any:
          - resources:
              kinds:
                - Namespace
      exclude:
        any:
          - resources:
              names:
                - kube-system
                - kube-public
                - kube-node-lease
      generate:
        generateExisting: true
        apiVersion: networking.k8s.io/v1
        kind: NetworkPolicy
        name: default-deny
        synchronize: true
        namespace: "{{request.object.metadata.name}}"
        data:
          spec:
            podSelector: {}
            policyTypes:
              - Ingress
              - Egress
