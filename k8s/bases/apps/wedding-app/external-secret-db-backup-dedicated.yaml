# Project the staged, bucket-scoped Wedding backup identity independently of the
# shared platform backup credential. The Cluster does not use it until the
# predecessor catalog has been mirrored and the cutover gate has passed.
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wedding-db-backup-r2-dedicated
  namespace: wedding-app
  labels:
    app.kubernetes.io/managed-by: ksail
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: wedding-db-backup-r2-dedicated
    creationPolicy: Owner
    template:
      data:
        ACCESS_KEY_ID: "{{ .ACCESS_KEY_ID }}"
        SECRET_ACCESS_KEY: "{{ .SECRET_ACCESS_KEY }}"
        REGION: "auto"
  data:
    - secretKey: ACCESS_KEY_ID
      remoteRef:
        key: apps/wedding-app/backup/r2
        property: access_key_id
    - secretKey: SECRET_ACCESS_KEY
      remoteRef:
        key: apps/wedding-app/backup/r2
        property: secret_access_key
