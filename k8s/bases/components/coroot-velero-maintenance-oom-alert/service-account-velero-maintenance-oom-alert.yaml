# Identity for the Velero maintenance OOM detector. Its only API grant is the
# namespaced Role in role-velero-maintenance-oom-alert.yaml.
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: velero-maintenance-oom-alert
  namespace: observability
