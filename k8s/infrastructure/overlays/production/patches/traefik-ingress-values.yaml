apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: traefik-ingress
  namespace: traefik-ingress
spec:
  values:
    ingressRoute:
      dashboard:
        matchRule: Host(`traefik.devantler.com`)