apiVersion: traefik.io/v1alpha1
kind: Ingress
metadata:
  name: dashboard
  namespace: traefik-ingress
spec:
  rules:
  - http:
      paths:
      - path: /dashboard
        pathType: Prefix
        backend:
          service:
            name: traefik-ingress
            port:
              number: 9000
