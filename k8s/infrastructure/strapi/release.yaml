apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: strapi
spec:
  interval: 5m
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  chart:
    spec:
      chart: strapi
      version: 10.0.21
      sourceRef:
        kind: HelmRepository
        name: strapi
  # https://github.com/truecharts/charts/blob/master/charts/stable/strapi/values.yaml
  values: {}
