apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: post-build
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./k8s/cluster/post-build
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
