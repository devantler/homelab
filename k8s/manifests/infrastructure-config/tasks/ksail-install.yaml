apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: ksail-install
  namespace: tekton
spec:
  steps:
    - name: ksail-install
      image: alpine
      script: |
        #!/bin/sh
        brew tap devantler/formulas
        brew install ksail
