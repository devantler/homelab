apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: ksail-down
  namespace: tekton
spec:
  steps:
    - name: echo
      image: alpine
      script: |
        #!/bin/sh
        echo "Hello World"
