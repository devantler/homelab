apiVersion: tekton-pipelines.dev/v1beta1
kind: Task
metadata:
  name: checkout
  namespace: tekton-pipelines
spec:
  steps:
    - name: echo
      image: alpine
      script: |
        #!/bin/sh
        echo "Hello World"
