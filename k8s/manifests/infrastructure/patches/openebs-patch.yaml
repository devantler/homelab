apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openebs
  namespace: openebs
spec:
  # https://github.com/openebs/openebs/blob/main/charts/values.yaml
  values:
    localpv-provisioner:
      hostpathClass:
        isDefaultClass: true
