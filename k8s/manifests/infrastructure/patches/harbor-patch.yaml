apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: harbor
  namespace: harbor
spec:
  dependsOn:
    - name: rook-ceph
      namespace: rook-ceph
    - name: rook-ceph-cluster
      namespace: rook-ceph
