apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: post-build-variables
  namespace: flux-system
spec:
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./k8s/clusters/production/post-build-variables
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg

