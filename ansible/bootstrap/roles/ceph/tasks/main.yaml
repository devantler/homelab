- name: check cephadm is present
  stat:
    path: /usr/sbin/cephadm
  register: cephadm
  when: ansible_hostname == "rpi1"

- name: setup ceph
  import_tasks: setup-ceph.yaml
  when:
  - ansible_hostname == "rpi1"
  - not cephadm.stat.exists