- name: Bootstrap new nodes
  hosts: all
  remote_user: devantler
  become: yes
  vars_files:
    - vars/main.yaml
  roles:
    # Node setup
    - poe-hat-fan-speeds
    - static-ips
    - hostnames
    - ssh-keys
    # Software setup
    - update
    - longhorn
    - geerlingguy.security
    #- ceph
    - reboot