- name: Bootstrap new nodes
  hosts: nodes-ips
  remote_user: devantler
  become: yes
  vars_files:
    - vars/main.yml
  roles:
    - static-ips
    - hostnames
    - poe-hat-fan-speeds
    - geerlingguy.git
    - geerlingguy.pip
    - geerlingguy.docker_arm
    - geerlingguy.security
    - hashicorp
  tasks:
    - name: Reboot nodes
      reboot: