- name: Bootstrap new nodes
  hosts: nodes-ips
  remote_user: devantler
  become: yes
  vars_files:
    - vars/main.yml
  roles:
    - geerlingguy.git
    - geerlingguy.pip
    - geerlingguy.docker_arm
    - geerlingguy.security
    - static-ips
    - poe-hat-fan-speeds
  tasks:
    - name: Reboot nodes
      reboot: