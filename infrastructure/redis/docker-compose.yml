networks:
  internal:
    external: true

services:
  server:
    image: redis:7.0.10-alpine
    restart: unless-stopped
    ports:
      - 6379:6379
    networks:
      - internal
    volumes:
      - data:/data
    healthcheck:
      test: redis-cli ping || exit 1

volumes:
  data:
