machine:
  kubelet:
    extraMounts:
      - destination: /var/mnt
        type: bind
        source: /var/mnt
        options:
          - bind
          - rshared
          - rw

  disks:
      - device: /dev/sdX # replace with correct disk
        partitions:
          - mountpoint: /var/mnt
