apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: longhorn
spec:
  releaseName: longhorn
  interval: 1h
  timeout: 15m
  chart:
    spec:
      chart: longhorn
      version: v1.3.1
      sourceRef:
        kind: HelmRepository
        name: longhorn
        namespace: flux-system
  