apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: tls-certificate-production
  namespace: default
spec:
  secretName: tls-certificate-production
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "*.devantler.com"
  dnsNames:
  - "devantler.com"
  - "*.devantler.com"