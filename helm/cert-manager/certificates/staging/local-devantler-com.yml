apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: local-devantler-com
  namespace: default
spec:
  secretName: local-devantler-com-staging-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  commonName: "*.local.devantler.com"
  dnsNames:
  - "local.devantler.com"
  - "*.local.devantler.com"